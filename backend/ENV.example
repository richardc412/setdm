# Unipile API Configuration
# Get these from your Unipile dashboard: https://app.unipile.com
UNIPILE_DSN=https://api.unipile.com:13420
UNIPILE_API_KEY=your-unipile-api-key-here

# Webhook Configuration
# Your public-facing URL where Unipile will send webhook events
# For local development, use ngrok or similar service to expose localhost
# Example: WEBHOOK_BASE_URL=https://your-ngrok-url.ngrok.io
# For production: WEBHOOK_BASE_URL=https://yourdomain.com
WEBHOOK_BASE_URL=

# Database Configuration
# PostgreSQL connection string for async database access
# Format: postgresql+asyncpg://username:password@host:port/database_name
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/setdm_db

# Application Settings
# Set to true for development to enable debug logging
DEBUG=false

# JWT Authentication
# IMPORTANT: Change this secret key in production! Use a strong random string.
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# AI Assist Configuration
# Provide your OpenAI API key to enable AI-assisted message generation
OPENAI_API_KEY=
# Optional: override the default lightweight model used for suggestions
OPENAI_MODEL=gpt-4o-mini
# Maximum number of historical messages to include when building context
OPENAI_HISTORY_LIMIT=20
# Default system prompt used when Autopilot sends automatic replies
AUTOPILOT_PROMPT=You are SetDM Autopilot. Craft concise, friendly replies that move conversations toward a meeting.

